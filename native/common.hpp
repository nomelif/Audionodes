
#ifndef COMMON_HPP
#define COMMON_HPP

#include <vector>
#include <array>
#include <cmath>
#include <algorithm>

const size_t N = 1024;
const int rate = 44100;
typedef std::array<float, N> Chunk;
typedef int node_uid;

#endif
